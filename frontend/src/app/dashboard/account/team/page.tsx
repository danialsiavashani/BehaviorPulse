import { ComingSoon } from "@/components/account/coming-soon";

export default function TeamSettingsPage() {
  return (
    <ComingSoon
      title="Team members"
      description="Invite teammates and manage roles here, once role-based access is built."
    />
  );
}